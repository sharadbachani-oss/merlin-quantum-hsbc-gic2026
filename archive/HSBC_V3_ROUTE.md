# HSBC v3 — first-principles-native advantage of route

## Why v2 failed the gates

The +5.8 pp vs SMOTE result is real (40 seeds, p = 0.001) and stays in
the annex. It is quantum-flavored classical ML: a degree-3 landscape
fitted from fraud-feature moments, sampled on Dirac-3. The first-principles
operator sat in the **null** feature-map arm (+0.0023 AUPRC). The lift
vanishes by ten labels (*power of data*). That cannot pass
first-principles-physics or today’s-hardware native advantage.

## The remap (Mitsubishi shape)

Mitsubishi mapped EUV photoresist onto A(ω): the industrial object
**is** a real-time spectrum; the classical scalable route (imaginary
time + analytic continuation) is ill-posed; the device evolves on the
real axis and Fourier-transforms.

HSBC maps AML circular flow onto the same object, on a payment graph:

```
H[G] = κ · D_G − A_G
κ   = 3/(3−√5)     (structural spectral; not fitted)
```

When G is the three native coupled pairs this **is** the first-principles operator
(exact 3-CX prep, E0 = −3δ, gap = δ). Extra payment edges are a
quench. FATF rings are short cycles on the native loop sector [4, 4, 6].

## What the device does that classical cannot cheaply fake

1. **Route.** S(ω) from real-time L(t) + FFT. The scalable classical
   route is G(τ) + continuation — two spectra with TV = 1 agree in G
   to 7×10⁻⁸ (model gate).
2. **Loops.** An AML ring wraps a cut at t = 1 (+0.721 nats vs a path)
   before the path light-cone arrives. That is the TN wall, and it is
   the object.
3. **Resonance.** A girth-6 ring puts 18.7% of S(ω) on Δs = 0.486;
   a girth-4 ring and a same-count smurf star put ~0.02%. The Ω
   sector is the detector.
4. **Noise as instrument.** Cycle-parity dephasing protects the ring
   echo 6.86× vs isotropic (2.2× the native tile). The device’s T2
   is the filter.

## What we do not claim

No AUPRC supremacy. No new S(ω) hardware shots. Prior IBM / Aquila /
Dirac jobs are a different protocol and live in the annex.
